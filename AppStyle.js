import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({

	container: {
	flex: 1,
		
	 },
	 imageBg: {
		flex: 1,
		resizeMode: 'contain',
		alignItems: 'center',
		justifyContent: 'flex-end',
	},

})